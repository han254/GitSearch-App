import { SearchAppDirective } from './search-app.directive';

describe('SearchAppDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchAppDirective();
    expect(directive).toBeTruthy();
  });
});
